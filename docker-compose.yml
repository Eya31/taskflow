services:
  backend:
    build: ./backend
    container_name: taskflow-backend
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    container_name: taskflow-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
